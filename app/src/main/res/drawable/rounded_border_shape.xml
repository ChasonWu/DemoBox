<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
             android:shape="rectangle">

    <!-- 填充 -->
    <solid android:color="@color/top_backgroud" />

    <!-- 对应颜色渐变 -->
    <gradient
        android:startColor="@color/top_backgroud"
        android:centerColor="@color/MyGray"
        android:endColor="@color/Mywhite_gray"
    android:angle="270"/>

    <!-- 描边 -->
    <!-- <stroke android:width="1dp"/> -->

    <!-- 圆角 -->
    <corners android:radius="5dp" />

    <padding
        android:top="6dp"
        android:bottom="5dp" />

</shape>